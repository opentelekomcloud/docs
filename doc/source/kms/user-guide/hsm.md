# HSM<a name="kms_01_0008"></a>

A hardware security module \(HSM\) is a hardware device that securely produces, stores, manages, and uses CMKs. In addition, it provides encryption processing services.

